{% extends 'admin/dashboard.html.twig' %}

{% block title %}Edit Game - Admin{% endblock %}

{% block admin_content %}
<div class="page-header">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h1 class="page-title">Edit Game</h1>
            <p class="page-subtitle">Update game information and settings</p>
        </div>
        <a href="{{ path('admin_jeu_index') }}" class="btn btn-outline">
            <i class="fas fa-arrow-left me-2"></i>Back to List
        </a>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <h5 class="mb-0"><i class="fas fa-edit me-2"></i>Game Information</h5>
    </div>
    <div class="card-body">
        {{ include('jeu/admin/_form.html.twig', {'button_label': 'Update Game'}) }}
    </div>
</div>
{% endblock %}
