<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Admin Dashboard{% endblock %}</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary: #6366f1;
            --primary-light: #818cf8;
            --primary-dark: #4f46e5;
            --secondary: #8b5cf6;
            --success: #10b981;
            --warning: #f59e0b;
            --danger: #ef4444;
            --dark: #0f172a;
            --darker: #0a0f1c;
            --light: #f8fafc;
            --gray: #64748b;
            --gray-light: #e2e8f0;
            --gray-dark: #334155;
            --sidebar: 260px;
            --radius: 12px;
            --radius-sm: 8px;
        }
        
        body { 
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            color: #e2e8f0;
            min-height: 100vh;
            overflow-x: hidden;
        }
        
        .navbar-admin { 
            background: rgba(15, 23, 42, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(148, 163, 184, 0.1);
            padding: 1rem 0;
        }
        
        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
            color: #fff;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .navbar-brand::before {
            content: '';
            width: 6px;
            height: 24px;
            background: linear-gradient(to bottom, var(--primary), var(--secondary));
            border-radius: 3px;
        }
        
        .sidebar {
            width: var(--sidebar);
            height: calc(100vh - 72px);
            background: rgba(15, 23, 42, 0.8);
            position: fixed;
            top: 72px;
            border-right: 1px solid rgba(148, 163, 184, 0.1);
            backdrop-filter: blur(10px);
            z-index: 1000;
            padding: 2rem 0;
            overflow-y: auto;   
            overflow-x: hidden;
        }
        
        .sidebar-header {
            padding: 0 1.5rem 1.5rem;
            border-bottom: 1px solid rgba(148, 163, 184, 0.1);
            margin-bottom: 1rem;
        }
        
        .sidebar-header h6 {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #94a3b8;
            font-weight: 600;
        }
        
        .nav-link {
            color: #94a3b8;
            padding: 0.75rem 1.5rem;
            margin: 0.25rem 1rem;
            border-radius: var(--radius-sm);
            transition: all 0.2s ease;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 12px;
        }
        
        .nav-link:hover {
            color: #e2e8f0;
            background: rgba(99, 102, 241, 0.1);
        }
        
        .nav-link.active {
            color: #fff;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            box-shadow: 0 4px 12px rgba(99, 102, 241, 0.2);
        }
        
        .nav-link i {
            width: 20px;
            text-align: center;
            font-size: 1.1rem;
        }
        
        .main-content {
            margin-left: var(--sidebar);
            padding: 2rem;
            min-height: calc(100vh - 72px);
        }
        
        .page-header {
            margin-bottom: 2rem;
        }
        
        .page-title {
            font-size: 1.875rem;
            font-weight: 700;
            color: #fff;
            margin: 0;
        }
        
        .page-subtitle {
            color: #94a3b8;
            font-size: 0.875rem;
            margin-top: 0.5rem;
        }
        
        .card {
            background: rgba(15, 23, 42, 0.7);
            border: 1px solid rgba(148, 163, 184, 0.1);
            border-radius: var(--radius);
            backdrop-filter: blur(10px);
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        
        .card:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        }
        
        .card-header {
            background: transparent;
            border-bottom: 1px solid rgba(148, 163, 184, 0.1);
            padding: 1.25rem 1.5rem;
            font-weight: 600;
            color: #fff;
        }
        
        .card-body {
            padding: 1.5rem;
        }
        
        .btn {
            border-radius: var(--radius-sm);
            font-weight: 500;
            padding: 0.625rem 1.25rem;
            transition: all 0.2s ease;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border: none;
            color: white;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(99, 102, 241, 0.3);
            color: white;
        }
        
        .btn-outline {
            background: transparent;
            border: 1px solid rgba(148, 163, 184, 0.2);
            color: #94a3b8;
        }
        
        .btn-outline:hover {
            background: rgba(255, 255, 255, 0.05);
            border-color: var(--primary);
            color: #fff;
        }
        
        .table {
            color: #e2e8f0;
            margin-bottom: 0;
        }
        
        .table thead {
            background: rgba(30, 41, 59, 0.5);
            border-bottom: 2px solid rgba(148, 163, 184, 0.1);
        }
        
        .table th {
            font-weight: 600;
            color: #94a3b8;
            text-transform: uppercase;
            font-size: 0.75rem;
            letter-spacing: 0.5px;
            padding: 1rem 1.5rem;
        }
        
        .table td {
            padding: 1rem 1.5rem;
            border-color: rgba(148, 163, 184, 0.1);
        }
        
        .table tbody tr {
            transition: background-color 0.2s ease;
        }
        
        .table tbody tr:hover {
            background: rgba(99, 102, 241, 0.05);
        }
        
        .badge {
            font-weight: 500;
            padding: 0.375rem 0.75rem;
            border-radius: 6px;
            font-size: 0.75rem;
        }
        
        .badge-admin {
            background: linear-gradient(135deg, #ef4444, #dc2626);
            color: white;
        }
        
        .badge-editor {
            background: linear-gradient(135deg, #f59e0b, #d97706);
            color: white;
        }
        
        .badge-user {
            background: linear-gradient(135deg, #64748b, #475569);
            color: white;
        }
        
        .badge-you {
            background: rgba(16, 185, 129, 0.2);
            color: #10b981;
            border: 1px solid rgba(16, 185, 129, 0.3);
        }
        
        .stat-card {
            text-align: center;
            padding: 1.5rem;
        }
        
        .stat-card .number {
            font-size: 2rem;
            font-weight: 700;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            line-height: 1;
        }
        
        .stat-card .label {
            color: #94a3b8;
            font-size: 0.875rem;
            margin-top: 0.5rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .dropdown-menu {
            background: rgba(15, 23, 42, 0.95);
            border: 1px solid rgba(148, 163, 184, 0.1);
            backdrop-filter: blur(10px);
            border-radius: var(--radius-sm);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
        }
        
        .dropdown-item {
            color: #e2e8f0;
            padding: 0.75rem 1rem;
            border-radius: var(--radius-sm);
            margin: 0.25rem 0.5rem;
            width: calc(100% - 1rem);
        }
        
        .dropdown-item:hover {
            background: rgba(99, 102, 241, 0.1);
            color: #fff;
        }
        
        .action-btn {
            width: 36px;
            height: 36px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: var(--radius-sm);
            background: transparent;
            border: 1px solid rgba(148, 163, 184, 0.2);
            color: #94a3b8;
            transition: all 0.2s ease;
        }
        
        .action-btn:hover {
            background: rgba(255, 255, 255, 0.05);
            color: #fff;
            transform: translateY(-1px);
        }
        
        .action-btn.view:hover {
            border-color: var(--primary);
            color: var(--primary);
        }
        
        .action-btn.edit:hover {
            border-color: var(--warning);
            color: var(--warning);
        }
        
        .action-btn.delete:hover {
            border-color: var(--danger);
            color: var(--danger);
        }
        
        @media (max-width: 768px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: static;
                border-right: none;
                border-bottom: 1px solid rgba(148, 163, 184, 0.1);
                padding: 1rem;
            }
            
            .main-content { 
                margin-left: 0;
                padding: 1rem;
            }
            
            .nav-link {
                margin: 0.125rem 0;
            }
        }
        
        /* Custom scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }
        
        ::-webkit-scrollbar-track {
            background: rgba(15, 23, 42, 0.5);
        }
        
        ::-webkit-scrollbar-thumb {
            background: rgba(99, 102, 241, 0.3);
            border-radius: 4px;
        }
        
        ::-webkit-scrollbar-thumb:hover {
            background: rgba(99, 102, 241, 0.5);
        }
        
        /* Status indicator */
        .status-indicator {
            display: inline-block;
            width: 8px;
            height: 8px;
            border-radius: 50%;
            margin-right: 8px;
        }
        
        .status-online {
            background: var(--success);
            box-shadow: 0 0 8px var(--success);
        }
        
        .status-offline {
            background: var(--gray);
        }
        
        /* Loading animation */
        @keyframes shimmer {
            0% { background-position: -200% center; }
            100% { background-position: 200% center; }
        }
        
        .shimmer {
            background: linear-gradient(90deg, 
                rgba(255,255,255,0) 0%, 
                rgba(255,255,255,0.05) 50%, 
                rgba(255,255,255,0) 100%);
            background-size: 200% 100%;
            animation: shimmer 2s infinite;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-admin">
        <div class="container-fluid px-4">
            <a class="navbar-brand" href="{{ path('admin_dashboard') }}">
                <span>Game Dashboard</span>
            </a>
            {% if app.user %}
            <div class="dropdown">
                <a class="nav-link dropdown-toggle text-white d-flex align-items-center gap-2" href="#" data-bs-toggle="dropdown">
                    <div class="status-indicator status-online"></div>
                    <span>{{ app.user.nom ?? 'Admin' }}</span>
                    <i class="fas fa-chevron-down fa-xs"></i>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                    <li><a class="dropdown-item" href="{{ path('admin_user_edit', {'id': app.user.id}) }}">
                        <i class="fas fa-user-edit me-2"></i>Edit Profile
                    </a></li>
                    <li><hr class="dropdown-divider my-2"></li>
                    <li><a class="dropdown-item" href="{{ path('app_logout') }}">
                        <i class="fas fa-sign-out-alt me-2"></i>Logout
                    </a></li>
                </ul>
            </div>
            {% endif %}
        </div>
    </nav>

    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-2 col-md-3 d-none d-md-block sidebar">
                <div class="sidebar-header">
                    <h6>Navigation</h6>
                </div>
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link {{ app.request.get('_route') == 'admin_dashboard' ? 'active' : '' }}" 
                           href="{{ path('admin_dashboard') }}">
                            <i class="fas fa-chart-line"></i>
                            <span>Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ 'admin_user' in app.request.get('_route') ? 'active' : '' }}" 
                           href="{{ path('admin_user_index') }}">
                            <i class="fas fa-users"></i>
                            <span>Users</span>
                        </a>
                    </li>
                
                   <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_product_index') }}">
                            <i class="fas fa-store"></i>
                            <span>Products</span>
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_category_index') }}">
                            <i class="fas fa-trophy"></i>
                            <span>Categories</span>
                        </a>
                    </li>
                    
                    <li class="nav-item">
                        <a class="nav-link {{ 'admin_match_game' in app.request.get('_route') ? 'active' : '' }}" href="{{ path('admin_match_game_index') }}">
                            <i class="fas fa-users"></i>
                            <span>Match_games</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ 'admin_stream' in app.request.get('_route') ? 'active' : '' }}" 
                        href="{{ path('admin_stream_index') }}">
                        <i class="fas fa-video"></i>
                          <span>Streaming</span>
                         </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ 'admin_equipe' in app.request.get('_route') ? 'active' : '' }}" href="{{ path('admin_equipe_index') }}">
                            <i class="fas fa-users"></i>
                            <span>Teams</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ 'admin_jeu' in app.request.get('_route') ? 'active' : '' }}" href="{{ path('admin_jeu_index') }}">
                            <i class="fas fa-gamepad"></i>
                            <span>Games</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ 'admin_tournoi' in app.request.get('_route') ? 'active' : '' }}" href="{{ path('admin_tournoi_index') }}">
                            <i class="fas fa-trophy"></i>
                            <span>Tournois</span>
                        </a>
                    </li>
                   <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_payement') }}">
                            <i class="fas fa-trophy"></i>
                            <span>Orders</span>
                        </a>
                    </li>
                     <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_blog_index') }}">
                            <i class="fas fa-trophy"></i>
                            <span>Blogs</span>
                        </a>
                    </li>
                                         <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_comment_index') }}">
                            <i class="fas fa-comment"></i>
                            <span>Comments</span>
                        </a>
                    </li>

                    
                    <div class="sidebar-header mt-4">
                        <h6>System</h6>
                    </div>
                    
                    <li class="nav-item">
                        <a class="nav-link" href="/">
                            <i class="fas fa-cog"></i>
                            <span>logout</span>
                        </a>
                    </li>
                    
                </ul>
            </div>

            <div class="col-lg-10 col-md-9 main-content">
                {% block admin_content %}{% endblock %}
            </div>
            
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
 
</body>
</html>