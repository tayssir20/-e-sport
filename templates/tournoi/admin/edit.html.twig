{% extends 'admin/dashboard.html.twig' %}

{% block title %}Modifier {{ tournoi.nom }} - Admin{% endblock %}

{% block admin_content %}
<div class="page-header">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h1 class="page-title">Modifier {{ tournoi.nom }}</h1>
            <p class="page-subtitle">Éditer les informations du tournoi</p>
        </div>
        <div class="d-flex gap-2">
            <a href="{{ path('admin_tournoi_show', {'id': tournoi.id}) }}" class="btn btn-outline">
                <i class="fas fa-eye me-2"></i>Voir
            </a>
            <a href="{{ path('admin_tournoi_index') }}" class="btn btn-outline">
                <i class="fas fa-arrow-left me-2"></i>Retour
            </a>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <h5 class="mb-0"><i class="fas fa-edit me-2"></i>Informations du tournoi</h5>
    </div>
    <div class="card-body">
        {{ include('tournoi/admin/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    </div>
</div>
{% endblock %}
