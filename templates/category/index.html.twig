<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Categories Management{% endblock %}</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

    <style>
        :root {
            --primary: #6366f1;
            --primary-light: #818cf8;
            --primary-dark: #4f46e5;
            --secondary: #8b5cf6;
            --success: #10b981;
            --warning: #f59e0b;
            --danger: #ef4444;
            --dark: #0f172a;
            --light: #f8fafc;
            --gray: #64748b;
            --sidebar: 260px;
            --radius: 12px;
            --radius-sm: 8px;
        }

        body { 
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            color: #e2e8f0;
            min-height: 100vh;
            overflow-x: hidden;
        }

        .navbar-admin { 
            background: rgba(15, 23, 42, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(148, 163, 184, 0.1);
            padding: 1rem 0;
        }

        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
            color: #fff;
        }

        .sidebar {
            width: var(--sidebar);
            height: calc(100vh - 72px);
            background: rgba(15, 23, 42, 0.8);
            position: fixed;
            top: 72px;
            border-right: 1px solid rgba(148, 163, 184, 0.1);
            padding: 2rem 0;
        }

        .sidebar-header {
            padding: 0 1.5rem 1rem;
            border-bottom: 1px solid rgba(148, 163, 184, 0.1);
            margin-bottom: 1rem;
        }

        .sidebar-header h6 {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #94a3b8;
            font-weight: 600;
        }

        .nav-link {
            color: #94a3b8;
            padding: 0.75rem 1.5rem;
            margin: 0.25rem 1rem;
            border-radius: var(--radius-sm);
            display: flex;
            align-items: center;
            gap: 12px;
            font-weight: 500;
            transition: all 0.2s ease;
        }

        .nav-link.active,
        .nav-link:hover {
            color: #fff;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
        }

        .main-content {
            margin-left: var(--sidebar);
            padding: 2rem;
            min-height: calc(100vh - 72px);
        }

        .page-title {
            font-size: 1.875rem;
            font-weight: 700;
            color: #fff;
            margin: 0;
        }

        .card {
            background: rgba(15, 23, 42, 0.7);
            border: 1px solid rgba(148, 163, 184, 0.1);
            border-radius: var(--radius);
        }

        .card-header {
            background: transparent;
            border-bottom: 1px solid rgba(148, 163, 184, 0.1);
            padding: 1.25rem 1.5rem;
            font-weight: 600;
            color: #fff;
        }

        .card-body {
            padding: 1.5rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border: none;
            color: white;
            padding: 0.625rem 1.25rem;
            border-radius: var(--radius-sm);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(99, 102, 241, 0.3);
        }

        .table {
            color: #e2e8f0;
        }

        .table thead {
            background: rgba(30, 41, 59, 0.5);
            border-bottom: 2px solid rgba(148, 163, 184, 0.1);
        }

        .table th, .table td { padding: 1rem 1.5rem; }

        .action-btn {
            width: 36px;
            height: 36px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: var(--radius-sm);
            background: transparent;
            border: 1px solid rgba(148, 163, 184, 0.2);
            color: #94a3b8;
            transition: all 0.2s ease;
        }

        .action-btn:hover { background: rgba(255,255,255,0.05); color: #fff; }
        .action-btn.view:hover { border-color: var(--primary); color: var(--primary); }
        .action-btn.edit:hover { border-color: var(--warning); color: var(--warning); }
        .action-btn.delete:hover { border-color: var(--danger); color: var(--danger); }

        @media (max-width: 768px) {
            .sidebar { width: 100%; height: auto; position: static; padding: 1rem; }
            .main-content { margin-left: 0; padding: 1rem; }
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-admin">
        <div class="container-fluid px-4">
            <a class="navbar-brand" href="{{ path('admin_dashboard') }}">Admin Dashboard</a>
            {% if app.user %}
            <div class="dropdown">
                <a class="nav-link dropdown-toggle text-white d-flex align-items-center gap-2" href="#" data-bs-toggle="dropdown">
                    <span>{{ app.user.nom ?? 'Admin' }}</span>
                    <i class="fas fa-chevron-down fa-xs"></i>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                    <li><a class="dropdown-item" href="{{ path('admin_user_edit', {'id': app.user.id}) }}">
                        <i class="fas fa-user-edit me-2"></i>Edit Profile
                    </a></li>
                    <li><hr class="dropdown-divider my-2"></li>
                    <li><a class="dropdown-item" href="{{ path('app_logout') }}">
                        <i class="fas fa-sign-out-alt me-2"></i>Logout
                    </a></li>
                </ul>
            </div>
            {% endif %}
        </div>
    </nav>
   <div class="container-fluid">
        <div class="row">
            <div class="col-lg-2 col-md-3 d-none d-md-block sidebar">
                <div class="sidebar-header">
                    <h6>Navigation</h6>
                </div>
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link {{ app.request.get('_route') == 'admin_dashboard' ? 'active' : '' }}" 
                           href="{{ path('admin_dashboard') }}">
                            <i class="fas fa-chart-line"></i>
                            <span>Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ 'admin_user' in app.request.get('_route') ? 'active' : '' }}" 
                           href="{{ path('admin_user_index') }}">
                            <i class="fas fa-users"></i>
                            <span>Users</span>
                        </a>
                    </li>
                
                   <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_product_index') }}">
                            <i class="fas fa-store"></i>
                            <span>Products</span>
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_category_index') }}">
                            <i class="fas fa-trophy"></i>
                            <span>Categories</span>
                        </a>
                    </li>
                    
                    <li class="nav-item">
                        <a class="nav-link" href= "{{ path('app_match_game_index2') }}"> >
                            <i class="fas fa-users"></i>
                            <span> Match_games</span>
                        </a>
                    </li>
                     <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_blog_index') }}">
                            <i class="fas fa-trophy"></i>
                            <span>Blogs</span>
                        </a>
                    </li>
                                         <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_comment_index') }}">
                            <i class="fas fa-comment"></i>
                            <span>Comments</span>
                        </a>
                    </li>

                    
                    <div class="sidebar-header mt-4">
                        <h6>System</h6>
                    </div>
                    
                    <li class="nav-item">
                        <a class="nav-link" href="/">
                            <i class="fas fa-cog"></i>
                            <span>logout</span>
                        </a>
                    </li>
                    
                </ul>
            </div>

            
        </div>
    </div>

            <main class="col-lg-10 col-md-9 ms-sm-auto main-content">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h1 class="page-title">Categories Management</h1>
                    <a href="{{ path('app_category_new') }}" class="btn btn-primary">
                        <i class="fas fa-plus me-2"></i>New Category
                    </a>
                </div>

                <!-- Total Categories Card -->
                <div class="row mb-4">
                    <div class="col-md-3">
                        <div class="card p-3 text-center">
                            <div class="number fs-3 fw-bold">{{ categories|length }}</div>
                            <div class="label text-muted">Total Categories</div>
                        </div>
                    </div>
                </div>


                <!-- Categories Table -->
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">All Categories</h5>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                   <tr>
                                    <th>ID</th>
                                    <th>Name</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for category in categories %}
                                <tr>
                                    <tr>
                                        <td>#{{ category.id }}</td>
                                        <td>{{ category.name }}</td>
                                    
                                        <td class="text-end">
                                            <a href="{{ path('app_category_show', {'id': category.id}) }}" class="action-btn view"><i class="fas fa-eye"></i></a>
                                            <a href="{{ path('app_category_edit', {'id': category.id}) }}" class="action-btn edit"><i class="fas fa-edit"></i></a>
                                            <form method="POST" action="{{ path('app_category_delete', {'id': category.id}) }}" class="d-inline" onsubmit="return confirm('Delete this category?')">
                                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
                                                <button type="submit" class="action-btn delete"><i class="fas fa-trash"></i></button>
                                            </form>
                                        </td>
                                    </tr>
                                    {% else %}
                                    <tr>
                                        <td colspan="7" class="text-center py-5 text-muted">
                                            <i class="fas fa-box fa-3x mb-3"></i>
                                            <p>No categories found</p>
                                            <a href="{{ path('app_category_new') }}" class="btn btn-primary">
                                                <i class="fas fa-plus me-2"></i>Add Category
                                            </a>
                                        </td>
                                    </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
