{% extends 'admin/dashboard.html.twig' %}

{% block title %}Create Product - Admin{% endblock %}

{% block admin_content %}
<div class="page-header">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h1 class="page-title">Create New Product</h1>
            <p class="page-subtitle">Add a new product to the system</p>
        </div>
        <a href="{{ path('admin_product_index') }}" class="btn btn-outline">
            <i class="fas fa-arrow-left me-2"></i>Back to Products
        </a>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <h5 class="mb-0"><i class="fas fa-plus-circle me-2"></i>Product Information</h5>
    </div>
    <div class="card-body">
        {{ include('product/admin/_form.html.twig') }}
    </div>
</div>
{% endblock %}
